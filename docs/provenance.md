# Provenance

## Computational Authority

- **Source:** `scripts/run_all.py` orchestrating `src/` modules (16 scientific modules)
- **Single computational path:** No alternative engines or competing implementations exist
- **Engine fidelity:** All `src/` modules are unmodified from the publication-hardened experimental authority

## Canonical Parameter Decisions

- **n_systems = 10,000** — Manuscript-declared value (para 14). The config file `parameters.yaml` originally contained 100,000 (aspirational); the execution script overrode to 10,000. This repository resolves the conflict by setting the canonical value to 10,000 in `parameters.yaml` and removing the override.
- **seed = 42** — Base random seed, declared in `parameters.yaml`
- **PYTHONHASHSEED = 0** — Set by `reproduce_all.py` via `config/harness_settings.json`

## Threshold Authority

- **Source:** `thresholds/threshold_profiles.yaml` (sole authority)
- **Profiles:** permissive, moderate, strict, very_strict
- **No competing threshold definitions exist**

## Data Sources

This study uses only synthetic data generated by the simulation framework. No real-world datasets are used. The system generator (`src/generators/system_generator.py`) creates synthetic AI systems from configured distributions.

## Reference Outputs

The `reference_outputs/` directory contains data outputs from the canonical run (seed=42, n_systems=10000, Python 3.11). These serve as validation comparison targets only and are not treated as reproduced outputs.

## Known Annotations

All manuscript numerical claims trace to computed outputs via rounding only. Zero logic discrepancies were identified across 40 traced claims (see Stage 0.5D traceability audit).
